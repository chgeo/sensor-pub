{
  "name": "sensor-pub",
  "version": "0.1.0",
  "description": "App for sensor data",
  "dependencies": {
    "@cap-js/sqlite": "^2",
    "@sap/cds": "^9",
    "express": "^4"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "private": true,
  "cds": {
    "requires": {
      "db": "sql",
      "[production]": {
        "db": "sqlite"
      }
    }
  }
}
