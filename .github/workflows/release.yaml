name: Release

on:
  workflow_dispatch:
  release:
    types: [published]

permissions:
  contents: read
  deployments: write

jobs:
  tests:
     uses: git@github.com:chgeo/sensor-pub/.github/workflows/test.yaml@main
     secrets: inherit
  deploy-cf:
    needs: [tests]
    uses: git@github.com:chgeo/sensor-pub/.github/workflows/cf.yaml@main
    secrets: inherit
    with:
      environment: Production
